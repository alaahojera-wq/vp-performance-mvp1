# VP Performance System (MVP) — Single-File Version

This version is designed to be **easy to upload to GitHub via the web UI** (no folders required).

## Deploy on Render (Web Service)
- Build Command:
  `pip install -r requirements.txt`
- Start Command:
  `uvicorn main:app --host 0.0.0.0 --port $PORT`

## Demo logins
- admin@local / admin123
- principal@local / principal123
- vp@local / vp123
